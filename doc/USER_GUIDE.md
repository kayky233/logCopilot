# LogPilot 用户手册

> 版本：v5.0 | 更新日期：2026-02

---

## 目录

1. [系统简介](#1-系统简介)
2. [快速开始](#2-快速开始)
3. [界面总览](#3-界面总览)
4. [详细操作指南](#4-详细操作指南)
   - 4.1 [配置 API 连接](#41-配置-api-连接)
   - 4.2 [上传日志文件](#42-上传日志文件)
   - 4.3 [上传故障手册](#43-上传故障手册)
   - 4.4 [选择手册与日志](#44-选择手册与日志)
   - 4.5 [启动分析](#45-启动分析)
   - 4.6 [查看诊断报告](#46-查看诊断报告)
5. [高级功能](#5-高级功能)
   - 5.1 [日志智能初筛](#51-日志智能初筛)
   - 5.2 [代码库挂载 (Code Agent)](#52-代码库挂载-code-agent)
   - 5.3 [Prompt 工程](#53-prompt-工程)
   - 5.4 [批量选择与通配符匹配](#54-批量选择与通配符匹配)
6. [手册编写规范](#6-手册编写规范)
7. [多用户与共享机制](#7-多用户与共享机制)
8. [部署方式](#8-部署方式)
9. [常见问题 (FAQ)](#9-常见问题-faq)

---

## 1. 系统简介

**LogPilot** 是一款面向通信基站运维团队的 AI 故障判决系统。它通过 **四个 AI Agent 协作流水线**，将故障手册知识与实际日志深度比对，自动输出结构化的故障诊断报告。

### 工作原理

```
故障手册 ─┐
          ├──→ Manual Agent ──→ Log Agent ──→ Code Agent ──→ Boss Agent ──→ 诊断报告
日志文件 ─┘      (提取判据)      (搜证)       (源码审计)     (综合判决)
```

| Agent | 职责 |
|-------|------|
| Manual Agent | 研读故障手册，提取关键判据和日志特征词 |
| Log Agent | 在日志中搜索手册中定义的故障特征，提取证据链 |
| Code Agent | (可选) 自动读取日志中报错位置的源码，辅助根因分析 |
| Boss Agent | 综合所有证据，输出最终的故障判决和修复建议 |

### 支持的领域

| 领域 | 代号 | 说明 |
|------|------|------|
| 板级支撑包 | BSP | 启动流程、驱动加载、内存管理、硬件抽象层 |
| 时钟同步 | CLK | GNSS、1588v2/PTP、PLL、SyncE、时钟源切换 |
| 交换网络 | SWITCH | VLAN、端口状态、风暴抑制、报文转发 |
| 通用 | OTHER | 其他自定义领域 |

---

## 2. 快速开始

**5 分钟内完成首次分析：**

### 第一步：启动系统

```bash
# 安装依赖 (首次)
pip install -r requirements.txt

# 启动
streamlit run app.py
```

浏览器会自动打开 `http://localhost:8501`。

### 第二步：配置 API

在左侧边栏 **⚡ API 连接** 中填入：

| 项目 | 示例值 | 说明 |
|------|--------|------|
| Base URL | `https://api.deepseek.com/v1` | 支持所有 OpenAI 兼容接口 |
| 模型 | `deepseek-chat` | 推荐 DeepSeek-V3 或 GPT-4o |
| API Key | `sk-xxxxxxxx` | 从模型提供商获取 |

点击 **💾 保存配置**。

### 第三步：上传文件

1. 在左侧 **📂 日志源** 上传待分析的日志文件（`.log`、`.txt`、`.xlsx`、`.csv`）
2. 在左侧 **📚 故障手册库** 选择领域后上传对应手册（`.md`、`.txt`、`.pdf`、`.docx`）

### 第四步：选择 & 分析

1. 在主界面 **Step 1** 勾选要使用的手册
2. 在 **Step 2** 勾选要分析的日志
3. 点击底部 **🚀 启动分析** 按钮

等待 AI 分析完成，即可查看诊断报告。

---

## 3. 界面总览

```
┌──────────────────────────────────────────────────────────────┐
│  侧边栏 (左)                    主界面 (右)                    │
│                                                              │
│  📡 LogPilot                   LogPilot (Hero 标题)           │
│  ─────────────                 状态徽章栏                      │
│  👤 用户 ID                                                   │
│     存储使用进度条              ┌─ Step 1: 选择排查手册 ─────┐ │
│                                │ 搜索 | 领域 | 全选 | 清空  │ │
│  ⚡ API 连接                   │ ☑ manual_clk_pll.md  [CLK] │ │
│     Base URL / 模型 / Key      │ ☑ manual_bsp_boot.md [BSP] │ │
│                                └────────────────────────────┘ │
│  📂 日志源                                                    │
│     上传日志 / 管理文件         ┌─ Step 2: 选择日志文件 ─────┐ │
│                                │ 搜索 | 全选 | 清空         │ │
│  📚 故障手册库 (共享)           │ ☑ log_pll_unlock.txt  1KB  │ │
│     领域选择 / 上传 / 管理      │ ☑ log_gps_fault.txt   2KB  │ │
│                                └────────────────────────────┘ │
│  🔬 高级设置                                                  │
│     初筛 / 代码库 / Prompt      已选摘要: 📚 2 手册 📄 2 日志  │
│                                                              │
│                                [🚀 启动分析 — 2×2 = 4 个任务] │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. 详细操作指南

### 4.1 配置 API 连接

展开左侧 **⚡ API 连接** 面板：

1. **Base URL**：填写 LLM 服务地址，支持所有 OpenAI 格式兼容接口：
   - DeepSeek：`https://api.deepseek.com/v1`
   - 通义千问：`https://dashscope.aliyuncs.com/compatible-mode/v1`
   - OpenAI：`https://api.openai.com/v1`
   - 本地 Ollama：`http://localhost:11434/v1`
   - 其他兼容接口均可使用

2. **模型名称**：根据所选服务商填写对应模型名：
   - `deepseek-chat`（推荐，性价比高）
   - `qwen-max`
   - `gpt-4o`
   - `llama3:8b`（Ollama 本地模型）

3. **API Key**：从对应模型服务商获取

4. 点击 **💾 保存配置** 按钮保存

> **提示**：配置会保存在本地，下次打开无需重新填写。不同用户 ID 的配置相互独立。

---

### 4.2 上传日志文件

展开左侧 **📂 日志源** 面板：

1. 点击上传区域或拖拽文件上传，支持格式：
   - `.log` / `.txt` — 纯文本日志
   - `.xlsx` / `.csv` — 表格格式日志（自动转为文本）

2. 支持批量上传多个文件

3. **管理文件**：点击 **🗑️ 管理文件** 可以选择性删除已上传的日志

> **限制**：单文件最大 50MB，每用户最多 100 个文件，总存储不超过 500MB。

> **注意**：日志文件是每个用户独立的，不同用户上传的日志互不可见。

---

### 4.3 上传故障手册

展开左侧 **📚 故障手册库 (共享)** 面板：

1. 先用下拉框选择目标**领域**（BSP / CLK / SWITCH / OTHER）
2. 上传手册文件，支持格式：`.md` / `.txt` / `.pdf` / `.docx`
3. 手册上传后会自动进入**全局共享库**，所有用户都能看到并使用

**重要说明：**
- 故障手册按领域分类，同一领域的手册被所有用户共享
- 任何用户上传的手册，其他用户也可以直接使用
- 这样设计的目的是让团队协作更高效——不同成员各自维护自己擅长领域的手册，全员受益
- 删除手册也是全局生效的，请谨慎操作

---

### 4.4 选择手册与日志

回到主界面，在两个步骤选择区进行勾选：

#### Step 1：选择排查手册

- **搜索**：在搜索框中输入关键词，快速过滤手册
- **领域筛选**：通过下拉框按领域筛选（ALL / BSP / CLK / ...）
- **全选 / 清空**：一键选中或取消全部手册
- **批量选择**：点击 ⚡ 使用通配符模式匹配，如 `*pll*` 会选中所有文件名含 `pll` 的手册

#### Step 2：选择日志文件

- 操作方式与手册选择相同
- 支持搜索、全选、清空、通配符批量操作
- 文件大小会显示在右侧，方便识别

#### 已选摘要

选择完成后，底部会显示摘要面板：

```
📚 手册 3  [CLK: 2] [BSP: 1]  |  📄 日志 2
              共 6 个分析任务
```

> **任务数 = 手册数 × 日志数**。每个日志会与每个手册分别进行一轮完整的 Pipeline 分析。

---

### 4.5 启动分析

确认选择后，点击底部的 **🚀 启动分析** 按钮。

分析过程：
1. 📚 解析手册 — 提取判据规则和特征词
2. 🔍 日志预处理 — 如启用初筛，先提取关键行
3. 🤖 Multi-Agent 分析 — 四个 Agent 依次协作
4. ✅ 输出结果 — 生成诊断卡片

进度条会实时显示当前处理阶段和整体进度。

---

### 4.6 查看诊断报告

每个分析任务会生成一张**结果卡片**：

#### 正常状态 🟢

```
🟢 ⏰ [CLK] manual_clk_pll.md                    正常
   置信度 ████████████████████████░░ 15%
```

#### 故障确认 🔴

```
🔴 ⏰ [CLK] manual_clk_pll.md                    故障确认
   📋 系统主时钟 PLL 失锁
   置信度 ██████████████████████████ 95%
   
   🔍 根因分析:
   日志中发现 PLL status changed: LOCK -> UNLOCK，
   与手册判据 #1 完全匹配...
   
   🔧 修复建议:
   1. 检查 GPS 天线连接是否正常
   2. 执行 MML 命令 LST CLKSTAT 查看当前时钟状态
   ...
```

#### 展开详情

- **📋 详细报告**：展开查看根因分析和修复建议的完整内容
- **🧠 AI 思考过程**：查看 Pipeline 各 Agent 的执行轨迹，包括：
  - 🕵️ Log Agent 的日志摘要
  - 💻 Code Agent 的源码审计结果
  - 🧠 Boss Agent 的最终推理
  - 📏 输入上下文长度

#### 扫描汇总

所有任务完成后，页面底部显示汇总仪表板：

| 总分析数 | 发现故障 | 正常 | 平均置信度 |
|----------|----------|------|-----------|
| 6        | 2        | 4    | 42%       |

---

## 5. 高级功能

### 5.1 日志智能初筛

位于左侧 **🔬 高级设置** 面板内。

**适用场景**：日志文件非常大（几十 MB），直接发给 AI 会超 Token 限制或消耗过多。

**使用方法**：
1. 开启 **启用关键词初筛** 开关
2. 编辑关键词列表（默认：`ERROR, FATAL, FAIL, EXCEPTION, TIMEOUT, 0x`）
3. 设置上下文行数（默认 5 行，即匹配行的前后各保留 5 行）

**工作原理**：
- 系统会在日志中搜索关键词，只提取匹配行及其前后上下文
- 大幅减少发送给 AI 的文本量，降低 Token 消耗
- 关键词不区分大小写，多个关键词用逗号分隔

> **建议**：对于小于 1MB 的日志，通常不需要开启初筛。

---

### 5.2 代码库挂载 (Code Agent)

位于左侧 **🔬 高级设置** → **代码库挂载**。

**适用场景**：日志中包含源码路径和行号信息（如 `src/driver/clk/clk_core.c:405`），想让 AI 自动读取对应源码辅助分析。

**配置方法**：
1. **代码根目录**：填写本地代码库的根路径，如 `D:\Code\BaseStation`
2. **服务器路径前缀**：填写日志中显示的服务器端路径前缀，如 `/usr1/BoardSoftware/Source/`
   - 系统会自动将日志中的路径 `/usr1/BoardSoftware/Source/driver/clk/clk_core.c` 映射到本地 `D:\Code\BaseStation\driver\clk\clk_core.c`
3. 开启 **启用 Code Agent** 开关

**效果**：
- AI 会自动读取报错位置的源码，理解代码上下文
- 在诊断报告中提供更精确的根因分析

> **注意**：不挂载代码库时，Code Agent 不会执行，不影响正常分析。

---

### 5.3 Prompt 工程

位于左侧 **🔬 高级设置** → **Prompt 工程**。

允许自定义 AI 的系统提示词和任务模板：

| Prompt | 用途 | 可编辑位置 |
|--------|------|-----------|
| System: BSP | BSP 领域 AI 人设与约束 | 高级设置 |
| System: CLK | CLK 领域 AI 人设与约束 | 高级设置 |
| System: SWITCH | SWITCH 领域 AI 人设与约束 | 高级设置 |
| System: OTHER | 通用领域 AI 人设与约束 | 高级设置 |
| Task: 默认模板 | 发送给 AI 的任务指令模板 | 高级设置 |

**自定义技巧**：
- 可以在 System Prompt 中加入更具体的领域知识和约束规则
- Task 模板中 `{manual_content}` 和 `{log_content}` 是占位符，会被实际内容替换
- 修改后点击 **💾 保存**，支持 **🔄 重置** 为默认值
- Prompt 文件存储在 `prompts/` 目录下，也可以直接编辑文件

---

### 5.4 批量选择与通配符匹配

手册和日志选择区都支持高效的批量操作：

| 操作 | 说明 |
|------|------|
| **全选** | 一键选中所有文件 |
| **清空** | 一键取消所有选中 |
| **搜索** | 实时过滤文件名 |
| **⚡ 批量选择** | 使用通配符模式，支持 `*` (任意字符) 和 `?` (单个字符) |

**通配符示例**：

| 模式 | 匹配 |
|------|------|
| `*pll*` | 所有含 `pll` 的文件 |
| `manual_clk_*` | 所有 CLK 领域手册 |
| `*.log` | 所有 `.log` 后缀文件 |
| `log_2026-02-*` | 2026 年 2 月的所有日志 |

---

## 6. 手册编写规范

手册质量直接决定分析效果。以下是推荐的编写规范：

### 推荐格式：Markdown

```markdown
# 故障定义：[故障名称]

## 告警信息
- 告警 ID：ALM-XXXX
- 告警名称：xxx
- 告警级别：紧急 / 重要 / 次要

## 日志特征 (判据)
以下日志特征出现时判定为故障：

1. `PLL status changed: LOCK -> UNLOCK`
   - 含义：PLL 锁相环失锁
   - 出现位置：clk_core 模块

2. `Fatal Error: System PLL lost lock, current_state=0x3`
   - 含义：系统级 PLL 失锁告警
   - 严重程度：紧急

## 触发条件
- 条件 1：GPS 天线开路或短路
- 条件 2：外部参考源信号质量劣化

## 排查步骤
1. 检查 GPS 天线连接状态
2. 执行 `LST CLKSTAT` 查看时钟状态
3. 检查参考源 SSM 等级

## 修复方法
1. 更换 GPS 天线
2. 重新配置参考源优先级
3. 必要时执行主备倒换
```

### 编写要点

| 要点 | 说明 |
|------|------|
| **日志特征要精确** | 使用代码块标记具体的日志匹配字符串，AI 会精确搜索 |
| **区分严重级别** | 标明判据对应的严重程度 |
| **给出排查步骤** | AI 会将排查步骤整合到修复建议中 |
| **一个手册一个故障** | 避免一份手册包含太多不同故障，会影响判断精度 |
| **使用结构化格式** | Markdown 标题层级清晰，AI 能更好地理解 |

---

## 7. 多用户与共享机制

### 用户隔离

在左侧 **👤 用户 ID** 中输入工号或姓名（如 `zhangsan`）即可切换用户。

| 资源 | 隔离方式 | 说明 |
|------|----------|------|
| 日志文件 | **用户独立** | 每个用户只能看到自己上传的日志 |
| 故障手册 | **全局共享** | 所有用户共享同一手册库 |
| API 配置 | **用户独立** | 每个用户有自己的 API Key 和模型配置 |
| Prompt | **全局共享** | System Prompt 和 Task 模板对所有用户一致 |
| 存储配额 | **用户独立** | 每个用户有独立的 500MB 存储限额 |

### 手册共享工作流

```
用户 A (CLK 专家) ──→ 上传 CLK 手册 ──┐
                                       ├──→ 共享手册库 ──→ 所有用户可用
用户 B (BSP 专家) ──→ 上传 BSP 手册 ──┘
                                       
用户 C (新人) ──→ 直接使用 A 和 B 上传的手册进行分析
```

> **最佳实践**：团队中各领域专家负责维护自己领域的手册，其他成员直接使用即可。

---

## 8. 部署方式

### 方式一：本地单机 (开发 / 演示)

```bash
pip install -r requirements.txt
streamlit run app.py
```

访问 `http://localhost:8501`

### 方式二：Docker 全栈 (团队 / 生产)

```bash
cp .env.example .env
# 编辑 .env，修改 SECRET_KEY（用于 JWT 签名，任意复杂字符串即可）

docker-compose -f docker/docker-compose.yml up -d
```

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端页面 | http://localhost | Streamlit UI (通过 Nginx 代理) |
| API 入口 | http://localhost/api/v1 | RESTful API |
| API 文档 | http://localhost:8000/docs | Swagger 交互式文档 |
| 直连前端 | http://localhost:8501 | 绕过 Nginx 直连 Streamlit |
| 直连后端 | http://localhost:8000 | 绕过 Nginx 直连 FastAPI |

### 方式三：手动启动后端 (开发调试)

```bash
pip install -r requirements-backend.txt
redis-server                                                      # Redis
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload     # FastAPI
celery -A backend.workers.celery_app worker -l info -Q analysis   # Celery
streamlit run app.py                                              # Streamlit
```

---

## 9. 常见问题 (FAQ)

### Q1：支持哪些 LLM 模型？

支持所有兼容 OpenAI API 格式的模型服务，包括：
- **DeepSeek**（推荐，性价比最高）
- **通义千问 / 百炼**
- **OpenAI GPT-4o / GPT-4**
- **本地部署的 Ollama**（完全免费，但效果取决于模型大小）

### Q2：分析一次大概消耗多少 Token？

取决于日志和手册大小：
- 小日志 (< 10KB) + 单手册：约 3,000 - 5,000 tokens
- 大日志 (1MB) + 开启初筛：约 8,000 - 15,000 tokens
- 启用 Code Agent 额外增加约 2,000 - 5,000 tokens

### Q3：日志太大怎么办？

两种解决方案：
1. **开启初筛**：在高级设置中启用关键词初筛，只提取关键行
2. **手动裁剪**：只上传关键时间段或关键模块的日志片段

### Q4：分析结果不准确怎么办？

可能的原因和解决方案：
1. **手册不够具体**：确保手册中有明确的日志特征字符串（参考第 6 节）
2. **模型能力不足**：尝试更强的模型（如 DeepSeek-V3、GPT-4o）
3. **Prompt 可优化**：在高级设置中调整 System Prompt，增加更具体的约束
4. **日志被过度裁剪**：关闭初筛，让 AI 看到更完整的日志上下文

### Q5：SECRET_KEY 怎么填？

`SECRET_KEY` 用于 JWT Token 签名，不是模型 API Key。随便填一个复杂的字符串即可，例如：

```
SECRET_KEY=my-super-secret-key-logpilot-2026
```

> 重要：生产环境请使用足够复杂的随机字符串，不要使用默认值。

### Q6：Docker 启动失败怎么办？

常见问题排查：
1. **Docker Desktop 未启动**：确保 Docker Desktop 正在运行
2. **镜像拉取超时**：在 Docker Desktop → Settings → Docker Engine 中配置国内镜像源
3. **端口被占用**：检查 80、8000、8501、6379 端口是否被其他程序占用

### Q7：怎么备份/迁移数据？

所有数据存储在 `analysis_workspace/` 目录下：
- `shared_manuals/`：共享手册库
- `workspaces/{user_id}/logs/`：用户日志
- `cache/`：LLM 结果缓存

复制此目录即可完成备份/迁移。

---

> **LogPilot** — 让每一次基站告警都有据可依 📡
